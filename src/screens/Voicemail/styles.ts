import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    viewVoicemail: {
        marginTop: '13%',
        marginLeft: '3%',
    },

    textVoicemail: {
        fontSize: 40,
        fontWeight: '700',
    },

    viewButton: {
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center',
    },

    buttonBorder: {
        borderWidth: 1,
        borderColor: "#8E8E8E",
        padding: 10,
        borderRadius: 6,
    },

    textButton: {
        color: "#8E8E8E",
        fontSize: 18,
    },
});

export { styles }